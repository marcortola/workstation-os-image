# Turn FreeType's stem darkening back on for both the CFF and autofit drivers.
# Fedora builds it off, which leaves the shell and terminal noticeably lighter
# than the previous base rendered them at the same size and DPI.
FREETYPE_PROPERTIES=cff:no-stem-darkening=0 autofitter:no-stem-darkening=0
