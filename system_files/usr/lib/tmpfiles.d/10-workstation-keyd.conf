# Image-owned defaults materialised from /usr/share/factory, so a user edit in
# /etc survives an image update. One file per feature: this held docker and keyd
# together, which meant neither name told you what the file was for.
d /etc/keyd 0755 root root -
L+ /etc/keyd/default.conf
