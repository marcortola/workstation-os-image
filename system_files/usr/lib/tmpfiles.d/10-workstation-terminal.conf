# Image-owned default terminal materialised from /usr/share/factory, so a user
# edit in /etc survives an image update. /etc/xdg is owned by the filesystem
# package and always exists, so no directory entry is needed.
L+ /etc/xdg/xdg-terminals.list
