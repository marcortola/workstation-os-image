# See the matching drop-in on gcr-ssh-agent.service. sockets.target is reached
# in the greeter's user manager, and this socket's ExecStartPost publishes
# SSH_AUTH_SOCK there for a session that will never use it.
[Unit]
ConditionUser=!@system
