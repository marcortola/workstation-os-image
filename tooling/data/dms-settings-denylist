# DMS settings excluded from generic interactive capture because they are
# runtime, device-specific, sensitive, path-bound, or require nested review.
# A narrow sanitized value may remain in the overlay after explicit review.
# Keep one exact top-level SettingsSpec key per line.
activeDisplayProfile
barConfigs
browserUsageHistory
connectedFrameBarStyleBackups
desktopClockDisplayPreferences
desktopClockCustomColor
desktopWidgetGroups
desktopWidgetInstances
desktopWidgetPositions
desktopWidgetGridSettings
displayProfiles
enabledGpuPciIds
filePickerUsageHistory
frameScreenPreferences
greeterSyncBaseline
greeterSyncPending
greeterWallpaperPath
hyprlandOutputSettings
lastAppliedIconTheme
lockScreenVideoPath
lockScreenWallpaperPath
matugenTargetMonitor
niriOutputSettings
screenPreferences
selectedGpuIndex
showOnLastDisplay
systemMonitorDisplayPreferences
systemMonitorGpuPciId
systemMonitorCustomColor

# Deliberately NOT denylisted, though they look machine-bound:
#   builtInPluginSettings  -- two keyboard triggers; tooling/dms/validate-overlay
#                             has a dedicated validator for its shape.
#   customThemeFile        -- templated with ${HOME} and the theme itself ships
#                             as a create_ seed, so it is portable by construction.
