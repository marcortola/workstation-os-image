# Toolchain and CLI. Homebrew needs a linker and a compiler for source builds.
binutils
gcc
cabextract                  # refresh-msttcore-fonts.sh, which the fonts installer runs
cpio
unzip
shadow-utils                # usermod, used by workstation-configure-user-groups
git-lfs                     # chained by the managed git post-checkout hook
mariadb
postgresql
mkcert
slurp                       # workstation-screenrecord
wf-recorder                 # workstation-screenrecord
